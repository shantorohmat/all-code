#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    int x,y,i = 0;
    while(t--)
    {
         i = 0;
        cin >> x >> y;
        cout<<powermod(n,ceil(log2(k)),mod)<<"\n";
        
    }
  
}
